query GetUser($userName: String!) {
	getUser(userName: $userName) {
		user {
			...userFragment
			images {
				id
				caption
				image_url
				likes
				like_status
				user_id
				created_at
				updated_at
			}
		}
		error {
			...userErrorFragment
		}
	}
}
