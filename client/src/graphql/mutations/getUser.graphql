query GetUser($username: String!) {
	getUser(username: $username) {
		user {
			...userFragment
			website
			bio
			private
			images {
				id
				caption
				image_url
				likes
				like_status
				created_at
			}
		}
		error {
			...userErrorFragment
		}
	}
}
