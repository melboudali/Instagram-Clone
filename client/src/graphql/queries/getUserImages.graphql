query GetUserImages($userId: Int!, $isPrivate: Boolean!, $limit: Int!, $cursor: String) {
	getUserImages(userId: $userId, isPrivate: $isPrivate, limit: $limit, cursor: $cursor) {
		hasMore
		images {
			...imageFragment
		}
	}
}
