query GetAllImages($limit: Int!, $cursor: String) {
  getAllImages(limit: $limit, cursor: $cursor) {
    hasMore
    images {
      id
      title
      url
      likes
      likeStatu
      userId
      user {
        id
        userName
        imageUrl
      }
      createdAt
      updatedAt
    }
  }
}
