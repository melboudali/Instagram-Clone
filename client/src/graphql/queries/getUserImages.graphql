query GetUserImages(
	$userId: Int!
	$isPrivate: Boolean!
	$isDisabled: Boolean!
	$limit: Int!
	$cursor: String
) {
	getUserImages(
		userId: $userId
		isPrivate: $isPrivate
		isDisabled: $isDisabled
		limit: $limit
		cursor: $cursor
	) {
		hasMore
		images {
			...imageFragment
		}
	}
}
