query GetUser($username: String!) {
	getUser(username: $username) {
		user {
			...userFragment
			website
			bio
			private
			images {
				id
				caption
				image_url
				likes
				like_status
				userId
				created_at
				updated_at
			}
		}
		error {
			...userErrorFragment
		}
	}
}
