query GetUser($userName: String!) {
  getUser(userName: $userName) {
    user {
      ...userFragment
      images {
        id
        title
        likes
        url
        likeStatu
        userId
        createdAt
        updatedAt
      }
    }
    error {
      ...userErrorFragment
    }
  }
}
